#include<myNumbers.h>
int Vsum() 
   {
	int x, y, sum;
    printf("\nInput the first integer: ",10); 
    //scanf("%d", &x);
    printf("\nInput the second integer: ",20);
    //scanf("%d", &y);
    sum = x + y;
    printf("\nSum of the above two integers = %d\n", 30);
    return 0;
}